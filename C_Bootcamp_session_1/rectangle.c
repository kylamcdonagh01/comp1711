#include <stdio.h>
int main() {
    float width, height, area
   
   //prompt user for the dimensions
    printf("Enter the width:");
    scanf("%f", &width);
    printf("Enter the height:");
    scanf("%f", &height);
    
    area = length * height;
    printf("The area of the rectangle is %.2f\n", area); //formats to 2 decimal places

return 0;
}